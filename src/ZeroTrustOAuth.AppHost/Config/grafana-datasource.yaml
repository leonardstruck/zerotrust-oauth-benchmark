apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: ${TEMPO_URL}
    uid: tempo
    editable: false
    jsonData:
      httpMethod: GET
      tlsSkipVerify: true
      nodeGraph:
        enabled: true